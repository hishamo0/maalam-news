# ملاحظات تطوير مستقبلية

## أولويات تحسين الموقع

1. مراجعة الصفحة الرئيسية بصرياً وضبط الأقسام والمسافات.
2. ضبط سلايدر "أحدث الأخبار" بعد المعاينة البصرية، خصوصاً الارتفاع والتوازن مع الشريط الجانبي.
3. تحسين قسم "تكنولوجيا" وتوحيد مسافات وحجم الكروت.
4. إصلاح البحث أو حذفه إذا لم يكن مطلوباً.
5. إضافة صفحة أخبار عامة `/news` تعرض كل الأخبار مع تنظيم أو فلترة.
6. تحسين الصور:
   - التأكد أن كل الصور موجودة في `public/images`.
   - إضافة `sizes` لكل صور `next/image` التي تستخدم `fill`.
   - توحيد أبعاد الصور قدر الإمكان.
7. تحسين SEO والروابط:
   - ربط التصنيفات بالمسارات الإنجليزية مثل `/category/technology`.
   - مراجعة `metadata` لكل صفحة.
   - التأكد من `sitemap` و `robots`.
